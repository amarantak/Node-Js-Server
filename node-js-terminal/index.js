const {generateRandomNumber,celciusToFarenheit } = require('./utils'); // Importing the function from utils.js

// Generating and logging a random number
console.log(`Random Number: ${generateRandomNumber()}`);

console.log(`Ceslcius to farenheit: ${celciusToFarenheit(0)}`);
